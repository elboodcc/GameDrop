<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>GameDrop - Descargas de Juegos</title>
    <style>
        body {
            background: #0d0d0d;
            font-family: Arial, Helvetica, sans-serif;
            margin: 0;
            color: white;
        }

        header {
            background: #111;
            padding: 20px;
            text-align: center;
            font-size: 28px;
            font-weight: bold;
            color: #00d4ff;
            letter-spacing: 1px;
        }

        .container {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 20px;
            padding: 20px;
        }

        .card {
            background: #1b1b1b;
            border-radius: 10px;
            overflow: hidden;
            box-shadow: 0 0 10px #000;
            transition: 0.3s;
        }

        .card:hover {
            transform: scale(1.03);
        }

        .card img {
            width: 100%;

